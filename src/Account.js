class Account {

    constructor(id, accountType, balance, customerId) {
        this.id = id;
        this.accountType = accountType;
        this.balance = balance;
        this.customerId = customerId;
    }
}